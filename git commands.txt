from django.urls import include, re_path
DIRS': [os.path.join(BASE_DIR,'templates')]

git init
git add .
git commit -m "first commit"
git branch - M master
git remote add origin "https://github.com/jacobsamraj1996/"
git push -u origin master
=========================================
#####To push our code to production branch

1. git pull
2. git push origin <own branch name>
3. then give pull request

####To pull others Code to our branch

1. git checkout <branch Name> common branch (ex: harish_02)
2. git pull
3. git checkout srinivasan
4. git merge <branch Name> common branch (ex: harish_02) 

####################check username ###########

git config user.name
git config user.email


change usename 


git config user.name "YourNewUsername"
git config user.email "YourNewEmail@example.com"
